
Place optional binaries here if you want the app to auto-start them:
- Windows: aria2c.exe, qbittorrent.exe
- macOS/Linux: aria2c, qbittorrent

Otherwise, configure remote endpoints in Settings (no binaries required).
